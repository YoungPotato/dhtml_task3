<!DOCTYPE html>
<html>
<head>
    <title>Гостевая книга</title>
    <meta charset="utf-8" />
    <style>
        body {
            margin: 0;
        }
        .input {
            width: 300px;
            height: 30px;
            margin: auto;
            font-size: 20px;
            text-align: center;
        }
        .form {
            width: 500px;
            margin: 200px auto 0 auto;
        }
        .text {
            width: 500px;
            height: 400px;
            overflow: scroll;
        }
        button {
            width: 150px;
            height: 25px;
        }
    </style>
</head>
<body>
    <form action="/" class="form" method="POST">
        <input class="input" name="str" type="text" required>
        <ul>
            <% for (let e of items) { %>
            <li><%-e%></li>
            <% } %>
        </ul>

        <button type="submit">Отправить</button>
    </form>
</body>
<html>